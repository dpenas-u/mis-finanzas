# Sin `base` fijo: Streamlit sigue el tema claro/oscuro del sistema, y así los
# widgets nativos (tablas, cabecera) quedan sincronizados con el CSS de la app.
[theme]
primaryColor = "#4f46e5"
font = "sans serif"

[server]
headless = true
